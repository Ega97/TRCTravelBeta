<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.trctravelbeta">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ActTai">

        </activity>
        <activity android:name=".driver.DetailOrderDriver" />
        <activity android:name=".driver.MainDriver" />
        <activity android:name=".LoginDriver" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpPenumpang" />
        <activity android:name=".Login" />
        <activity android:name=".Konfirmasi">

        </activity>
        <activity android:name=".DetailBooking" />
        <activity android:name=".Pembayaran" />
        <activity android:name=".DetailPenjemputan"></activity>
        <activity android:name=".PilihMobil" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>